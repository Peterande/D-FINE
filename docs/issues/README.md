# Research issues

This directory is the execution queue for the Tagtwo model-improvement program.

Agents must:

1. Start with `20260903-1341-ai-model-improvement-program.md`.
2. Follow `Depends on`; do not skip an unfinished hard dependency.
3. Keep `Status`, evidence, decisions, validation, and `Next step` current.
4. Separate measured local results from paper claims and filename-derived assumptions.
5. Keep datasets and large model artifacts outside Git; record immutable paths and hashes.
6. Do research here. Change `tagtwo-monorepo` only through the production-promotion issue after a
   candidate passes the agreed gates.

Statuses: `Open`, `In Progress`, `Blocked`, `Done`, `Dropped`.
